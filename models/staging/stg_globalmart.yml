version: 2

models:
  - name: stg_orders
    description: combine all tables for staging data and calculate profit
    columns:
      - name: orderid
        description: primary key, unique and not null for stg_orders
      - name: shipmode
        description: '{{doc("shipmode")}}'